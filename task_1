1. Дан код. Почему код выдает такие результаты.
var a = 1, b = 1, c, d;      // Объявление переменных и присвоение им значений.
c = ++a; alert(c);           // 2 - Сначала происходит присвоение значения в а (a + 1), затем значение возвращается в c. И c и a равны 2.
d = b++; alert(d);           // 1 - Сначала возвращается значение в переменную d, затем в b присваивается значение b + 1. d = 1, но b уже равно 2.
c = (2+ ++a); alert(c);      // 5 - Сначала в а происходит присвоение (а + 1), затем 2 + 3 = 5 присваивается в c. После вычисления и присвоения имеем с = 5, а = 3.
d = (2+ b++); alert(d);      // 4 - Сначала происходит вычисление 2 + b = 4 и данное значение присваивается в d. Затем в b присваивается значение (b + 1). d = 4, но b = 3. 
alert(a);                    // 3 - Выводим результат, хранящийся в переменной.
alert(b);                    // 3 - Выводим результат, хранящийся в переменной.
